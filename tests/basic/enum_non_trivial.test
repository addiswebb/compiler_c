// RUN: %cc %s -a -o %t.s
// RUN: gcc %t.s -o %t.exe
// RUN: pwsh -c "%t.exe ; %check_exit 51 $LASTEXITCODE"

enum E { A = 5, B = 0, C };

int main() {
    return A * 10 + B * 3 + C;   // 5*10 + 0 + 1 = 51
}
