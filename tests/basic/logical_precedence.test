// RUN: %cc %s -a -o %t.s
// RUN: gcc %t.s -o %t.exe
// RUN: pwsh -c "%t.exe ; %check_exit 1 $LASTEXITCODE"

int main() {
    int x = 1;
    int y = 0;
    int z = 2;

    int r = x || y & z;
    // precedence: & evaluated before ||
    // y & z = 0 & 2 = 0
    // x || 0 = 1

    return r;
}
