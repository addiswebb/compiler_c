// RUN: %cc %s -a -o %t.s
// RUN: gcc %t.s -o %t.exe
// RUN: pwsh -c "%t.exe ; %check_exit 3 $LASTEXITCODE"

int main() {
    int x = 2;
    float y = 3.5;
    {
        int x = (int)y + 1; // 4
        {
            x *= 2; // 8
        }
    }
    x += 1; // outer x = 3
    return x;
}
