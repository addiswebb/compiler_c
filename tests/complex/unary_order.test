// RUN: %cc %s -a -o %t.s
// RUN: gcc %t.s -o %t.exe
// RUN: pwsh -c "%t.exe ; %check_exit 73 $LASTEXITCODE"

int main() {
    int x = 1;
    int y = x++ + ++x + x;
    // x++ -> 1 (x=2)
    // ++x -> 3
    // x   -> 3
    // y = 1 + 3 + 3 = 7
    return y * 10 + x; // 70 + 3 = 73
}
