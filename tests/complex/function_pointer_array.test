// RUN: %cc %s -a -o %t.s
// RUN: gcc %t.s -o %t
// RUN: %check_exit 9 %t

int add1(int x) { return x + 1; }
int add2(int x) { return x + 2; }

int main() {
    int (*f[2])(int);
    f[0] = add1;
    f[1] = add2;

    return f + f; // 4 + 5
}